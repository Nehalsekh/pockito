name: "Kohli workflow"

on:
  push:
    branches:
      - backend_03062026

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "kohli"
        run: echo "Kohli"
